name: Build Nous Sport V18 HEALTH CONNECT

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Java 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      - name: Gradle 8.11.1
        uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: '8.11.1'

      - name: Extract exact V18 project
        shell: bash
        run: |
          set -euo pipefail
          unzip -q nous-sport-v18-healthconnect.zip -d v18
          ROOT=$(find v18 -type f -name settings.gradle -print -quit | xargs -r dirname)
          test -n "$ROOT"
          cd "$ROOT"
          gradle wrapper --gradle-version 8.11.1 --distribution-type bin
          ./gradlew clean assembleDebug --no-daemon

      - name: Upload V18 APK
        uses: actions/upload-artifact@v4
        with:
          name: NOUS-SPORT-V18-HEALTHCONNECT-APK
          path: v18/**/app/build/outputs/apk/debug/app-debug.apk
          if-no-files-found: error
